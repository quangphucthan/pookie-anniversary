# Supabase project settings -> Data API. Copy to .env.local and fill in.
# Both of these ship to the browser by design; RLS is what protects the data.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
